name: Create preprod infrastructure on AWS and run integration tests
on:
  push:
    branches:
      - master

jobs:
  create-preprod-infra:
    name: Create preprod infrastructure on AWS
    runs-on: self-hosted
    env:
      DOCKER_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKERHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v2
      - name: Python Package
        run: |
          make python-package
      - name: Terraform Init
        working-directory: ./terraform/stacks/list-reconciliation
        run: |
          terraform init
          terraform workspace select "preprod" || terraform workspace new "preprod"
      - name: Terraform Validate
        working-directory: ./terraform/stacks/list-reconciliation
        run: |
          terraform validate
      - name: Terraform Apply
        working-directory: ./terraform/stacks/list-reconciliation
        run: |
          terraform apply -auto-approve
      - name: Upload terraform output artifact
        uses: actions/upload-artifact@v2
        with:
          name: terraform-output
          path: output.json
          retention-days: 2
  run-integration-tests:
    name: Run integration tests
    runs-on: self-hosted
    needs:
      - create-preprod-infra
    steps:
      - uses: actions/checkout@v2
      - uses: actions/download-artifact@v2
        with:
          name: terraform-output
      - name: Assume Role in PreProd Account for Integration Tests
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-region: eu-west-2
          role-to-assume: "arn:aws:iam::287634746327:role/LRTerraformDeploy"
          role-duration-seconds: 1200
      - name: Run Integration tests
        run: |
          # Ensure gpextract and mock_data install location is available to Gauge
          export PATH=$PATH:/opt/github_runner/.local/bin
          make integrationtest-deps
          make integrationtests
      - name: Upload Integration tests result artifact
        if: always()
        uses: actions/upload-artifact@v2
        with:
          name: Test report
          path: test/integrationtests/reports/html-report/*
          retention-days: 2
      - name: Upload Integration tests logs artifact
        if: always()
        uses: actions/upload-artifact@v2
        with:
          name: Test logs
          path: test/integrationtests/logs/*
          retention-days: 2
