# Mock Data api Tests

## run mock api to generate gp and pds mock data

* check and remove if file "gpdata.json" exists
* run the mock api to generate "50" mock gprecords for key "e2f768c0"
* assert if the "gpdata.json" file keys exists
* assert the number of rows created is "50" in file "gpdata.json"
